# Frontend environment variables (local dev)
# Copy to: apps/frontend/.env.local

NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=dev-nextauth-secret-change-in-production

# API
NEXT_PUBLIC_API_URL=http://localhost:3001
# Optional (if/when websocket client uses it)
NEXT_PUBLIC_WEBSOCKET_URL=http://localhost:3001

# If your NextAuth adapter uses the database on the server side
DATABASE_URL="postgresql://devuser:devpassword@localhost:5432/nextnode_dev"
