// Re-exported from the shared server-side contracts location.
// Do not add new types here; add them to apps/frontend/src/server/bdd/types.ts instead.
export type {
  BddFeatureOverview,
  BddScenarioOverview,
  Snapshot,
  StatusCounts,
  StatusKey,
} from '@/src/server/bdd/types';
