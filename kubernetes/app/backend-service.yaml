apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: next-node-app
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: next-node-app-base
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 3001
      targetPort: http
