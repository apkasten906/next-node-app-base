apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: next-node-app
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: next-node-app-base
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend
  ports:
    - name: http
      port: 3000
      targetPort: http
